---

## [变更时间] 2026-07-17 10:23:28 author:wang yihui

### 📌 变更类型
修改 / 配置

### 📁 影响范围
- `.env`
- `deploy/split/.env.host.192.168.1.24.template`
- 宿主机远程基础设施、OCR 和 Sandbox 路径配置

### 📝 变更内容
将根目录私有 `.env` 的 PostgreSQL、Neo4j 和 MinIO 凭据切换为 `docker-compose.prod.yml` 中未修改的原始默认值，Redis 按原生产 Compose 的无认证模式连接，并生成新的本地 JWT 密钥；明确将 MinerU、PaddleX 和 PaddleOCR 配置为空，同时把 Sandbox 虚拟路径修正为容器内 `/home/gem/user-data`。可提交模板继续只保留秘密占位符。

### 🔍 变更原因
服务器 `192.168.1.24` 使用原生产 Compose 默认凭据，且当前开发场景不部署 OCR/文档解析服务，需要让本地宿主机配置与实际服务认证方式和启用范围保持一致。

### ⚠️ 注意事项
真实凭据只写入受 Git 忽略的根目录 `.env`，未写入模板、文档或变更日志。原生产 Compose 默认凭据只适合受控开发环境，正式生产前必须轮换。OCR 配置为空不代表解析器从代码中移除；当前环境不要选择 MinerU、PaddleX 或 PaddleOCR 解析器。Redis 当前无认证，只应通过可信内网访问。

### 🧪 测试建议
已脱敏校验本地 PostgreSQL、Neo4j、MinIO 配置与生产 Compose 默认值一致，Redis 使用无认证地址，JWT 已生成，OCR 字段为空，Sandbox 虚拟路径正确，且可提交模板未包含真实秘密。后续应启动 API/Worker，验证数据库登录、Redis连接、MinIO上传、Milvus检索和Neo4j查询。

---
