---

## [变更时间] 2026-07-17 10:01:54 author:wang yihui

### 📌 变更类型
新增 / 配置

### 📁 影响范围
- `deploy/split/.env.host.192.168.1.24.template`
- `.env`
- 宿主机本地开发与远程基础设施连接配置

### 📝 变更内容
新增面向基础设施服务器 `192.168.1.24` 的宿主机开发配置模板，预置 PostgreSQL、Redis、MinIO、Milvus、Neo4j 标准端口和本地 API/Web/Sandbox 配置，并使用占位符保留秘密值；同步更新根目录 `.env` 中的 Neo4j、Milvus、MinIO、NO_PROXY 和实例标识等非敏感配置。

### 🔍 变更原因
基础设施已经部署在 `192.168.1.24`，本地 API、Worker、Web 和 Sandbox Provisioner 需要使用统一且可复用的远程服务地址配置。

### ⚠️ 注意事项
根目录 `.env` 仍需在本机填写 PostgreSQL、Redis、Neo4j、MinIO、JWT 等秘密值，模板中的 `<...>` 不能直接用于启动程序。密码含特殊字符时必须在 PostgreSQL/Redis URL 中进行百分号编码。根目录 `.env` 受 Git 忽略，不应提交真实凭据。

### 🧪 测试建议
已验证开发机可以访问服务器的 5432、6379、9000、9001、19530、9091、7474、7687 端口，MinIO、Milvus 和 Neo4j HTTP 健康接口返回 200。补齐秘密值后应执行宿主机检查脚本，并验证 API、Worker、对象上传、向量检索和图数据库连接。

---
